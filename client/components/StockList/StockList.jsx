import React from 'react';
// import { Button, Input, Row } from 'react-materialize';

const StockList = props => (
  <div>
    {/* render list of StockItem */}
  </div>
);

export default StockList;
